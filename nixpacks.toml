[phases.setup]
nixPkgs = ["python311", "libpq", "gcc"]

[phases.install]
cmds = ["pip install -r reqs.txt"]

[phases.build]
cmds = []

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
